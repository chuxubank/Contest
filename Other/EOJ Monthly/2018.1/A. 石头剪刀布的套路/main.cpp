//TODO: All win cases.
#include <bits/stdc++.h>
using namespace std;

struct S
{
	int m;
	int c;
	S(int m, int c) : m(m), c(c) {}
};

int win(int a)
{
	return (a % 3 + 1);
}

int pri(int a, int b)
{}

int main()
{
	int p, q, r;
	cin >> p >> q >> r;
	S[] = { S(1, 0), S(2, 0), S(3, 0) };

	return 0;
}
